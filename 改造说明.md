# LAST LIGHT｜僵尸街区

这是基于 [Operation Ironhold](https://github.com/StarKnightt/operation-ironhold) 的网页 FPS 改造原型。

## 许可证

上游项目为 MIT 许可证。`LICENSE` 原文件保留在项目根目录，`index.original.html` 是未改动的上游基线。后续发布或分发时必须继续保留 MIT 版权和许可声明。

## 当前第一轮改造

- 保留 Three.js、第一人称镜头、WASD、跳跃、蹲伏、奔跑、ADS、换弹、命中反馈和原有 QA 工具。
- 开始界面改成 `LAST LIGHT // ZOMBIE DISTRICT`。
- 增加低多边形城市街区外立面、窗带、路灯和冷暖灯光。
- 敌人外观改成绿色感染者，隐藏敌方枪械，改为追击和近距离攻击。
- 增加 `4` 键手雷：投掷、抛物线、落地爆炸、范围伤害和爆炸反馈。
- 增加 `?test=1` 自动测试模式，用于不依赖 Pointer Lock 地检查计时、敌人攻击、失败重开和手雷输入。

## 当前第二轮改造

- 接入用户提供的 `m9_bayonet_knife.glb` 和 `m67_hand_grenade.glb`，替换程序生成的刀具与手雷外观。
- `3` 键拿刀：鼠标左键快速轻击，右键蓄力重击；持刀右键不会触发开镜。
- `4` 键只切换到手雷，鼠标左键才会投掷，避免切换装备时误投。
- 每波敌人清除后进入 5 秒休整，中央显示中文倒计时，随后生成数量更多的下一波。
- 增加仅在 `?test=1&selftest=1` 下运行的战斗循环回归测试，不影响正常试玩。

## 当前运行

在项目目录执行：

```powershell
python -m http.server 5189
```

然后打开：

```text
http://127.0.0.1:5189/index.html
```

自动测试模式：

```text
http://127.0.0.1:5189/index.html?test=1
```

## 后续可继续扩展

- 使用 Quaternius 城市和 Ultimate Gun 的 GLB/GLTF 资源替换关键占位模型。
- 加入音效资源或继续扩展 Web Audio 合成音效。
- 根据试玩反馈继续微调刀具动作速度、僵尸血量和后期波次上限。
